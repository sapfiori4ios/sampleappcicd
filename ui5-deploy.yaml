# yaml-language-server: $schema=https://sap.github.io/ui5-tooling/schema/ui5.yaml.json

specVersion: "2.6"
metadata:
  name: com.sample.sampleapp
type: application
builder:
  resources:
    excludes:
      - /test/**
      - /localService/**
  customTasks:
    - name: deploy-to-abap
      afterTask: generateCachebusterInfo
      configuration:
        target:
          url: http://fgtappg4b.fulcrum-gt.com:8000/
          client: "100"
        app:
          name: TMP
          description: TMP
          package: SAMPLE
          transport: TR102920209
